# build via glibc and native bootstrap musl
sys-devel/binutils:               -gold
sys-devel/gcc*:                   -bootstrap -openmp -lto -static

sys-libs/musl*:                   +glibc

# wrapper <musl-gcc> not support c++
*/*: -cxx
